# Recorrido funcional por roles y caza de fallos

Rama: `claude/busy-bohr-000kvi` (sale de `nube/base`, 8b4219f).
Informe para el dueño del liceo, en curso. Se actualiza en cada arreglo que queda en verde.

## Estado

**Borrador.** Preparando el entorno (PostgreSQL, Redis, dependencias, liceo de pruebas).
La línea base (cuántas pruebas pasan antes de tocar nada) se apunta aquí en cuanto esté medida.

## Línea base (antes de tocar nada)

| Tanda | Pasan | Fallan | Saltadas | Notas |
|---|---|---|---|---|
| Servidor (jest de integración) | … | … | … | |
| Tipos del servidor (`typecheck`) | … | | | |
| Tipos de la web (`typecheck`) | … | | | |
| Web (jest) | … | … | … | |
| Navegador (Playwright) | … | … | … | |
| Teléfono (`npm run movil -- --exigir`) | … | | | |

## Qué estaba mal y qué le pasaba al liceo

(pendiente)

## Qué se arregló

(pendiente)

## Qué se probó y qué no

(pendiente)

## Qué queda por decidir

(pendiente)
